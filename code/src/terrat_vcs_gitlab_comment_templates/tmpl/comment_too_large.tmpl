The output of this operation exceeds the GitLab comment size and cannot be displayed.
@^work_manifest_url-@

To see the output in the Stategraph Console, click [here](@work_manifest_url@).
@/work_manifest_url-@

To continue, do any of the following:

- If the pull request contains changes in multiple directories, reduce the output size by running directories in separately (see the documentation for [plan](https://stategraph.com/docs/advanced-workflows/tags/#commands) and [apply](https://stategraph.com/docs/advanced-workflows/tags/#commands) or use a tag query (see example for [plan](https://stategraph.com/docs/advanced-workflows/tags/#commands) or [apply](https://stategraph.com/docs/advanced-workflows/tags/#commands)).
- Review the GitLab CI log.  The GitLab CI log contains all of the Terraform output.
- Reduce the number of changes in the pull request.

For further support:

- Join our [Slack community](https://slack.stategraph.com/) (Fastest)
- Email us directly at support@@stategraph.com
