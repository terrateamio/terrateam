(library
 (name terrat_vcs_service_gitlab_ee)
 (libraries
  pgsql_io
  sln
  terrat_vcs_access_token
  terrat_vcs_provider2
  terrat_vcs_service
  terrat_vcs_service_gitlab)
 (preprocessor_deps
  (glob_files sql/*.sql))
 (preprocess
  (pps ppx_blob)))
