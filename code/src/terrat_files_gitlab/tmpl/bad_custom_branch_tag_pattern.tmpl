## Malformed Branch Tag Pattern

There is an error in the repository configuration with a malformed branch tag pattern, in the `tags` section of the configuration.


The tag is in the `@tag@` configuration and the malformed pattern is `@pattern@`.
