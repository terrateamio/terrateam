Cannot perform action because some directories and workspaces are owned by other pull requests.

| Dir | Workspace | Owner |
| --- | --------- | ----- |
@#dirspaces-@
| `@dir@` | `@workspace@` | #@pull_request_id@ |
@/dirspaces-@

To allow this change to be applied, you can comment `terrateam unlock` in **each of the listed pull requests** to remove any locks on directories that they have.
