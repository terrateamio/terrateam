insert into installation_config (
       installation_id,
       allow_draft_pr,
       auto_merge_after_apply,
       autoplan_file_list,
       default_terraform_version,
       enable_apply,
       enable_apply_all,
       enable_autoplan,
       enable_diff_markdown_format,
       enable_local_merge_dest_branch_before_plan,
       enable_repo_locking,
       enable_terragrunt,
       require_approval,
       require_mergeable,
       updated_at,
       updated_by
)
values (
       $installation_id,
       $allow_draft_pr,
       $auto_merge_after_apply,
       $autoplan_file_list,
       $default_terraform_version,
       $enable_apply,
       $enable_apply_all,
       $enable_autoplan,
       $enable_diff_markdown_format,
       $enable_local_merge_dest_branch_before_plan,
       $enable_repo_locking,
       $enable_terragrunt,
       $require_approval,
       $require_mergeable,
       now(),
       $user_id
)
on conflict (installation_id) do update set (
   allow_draft_pr,
   auto_merge_after_apply,
   autoplan_file_list,
   default_terraform_version,
   enable_apply,
   enable_apply_all,
   enable_autoplan,
   enable_diff_markdown_format,
   enable_local_merge_dest_branch_before_plan,
   enable_repo_locking,
   enable_terragrunt,
   require_approval,
   require_mergeable,
   updated_at,
   updated_by
) = (
  excluded.allow_draft_pr,
  excluded.auto_merge_after_apply,
  excluded.autoplan_file_list,
  excluded.default_terraform_version,
  excluded.enable_apply,
  excluded.enable_apply_all,
  excluded.enable_autoplan,
  excluded.enable_diff_markdown_format,
  excluded.enable_local_merge_dest_branch_before_plan,
  excluded.enable_repo_locking,
  excluded.enable_terragrunt,
  excluded.require_approval,
  excluded.require_mergeable,
  excluded.updated_at,
  excluded.updated_by
)
